# 作業ログ

## 2026-04-29 12:48
- グループ化機能を一旦削除
  - UIおよび関連するロジックをクリーンアップ
- アイコンクリック時に画面が変色（エラーでクラッシュ）する問題を修正
  - `ToolSettingsPanel` で使用していた `Boxes` と `Ungroup` アイコンのインポート漏れを修正
- 「前面へ」「背面へ」の操作が正しく動作しない問題を修正
  - Fabric.js の `sendObjectBackward` / `bringObjectForward` が重なり（交差）判定によって動作しない場合があるため、`moveObjectTo` を使用した確実なインデックス操作に切り替え
- アプリが真っ白になるバグを修正
  - `ToolbarButton` コンポーネント内でスコープ外の `toolSettings` を参照していた `ReferenceError` を解消
  - `ToolbarButton` のプロパティに `layerActions` の分割代入が漏れていた箇所を修正
  - 選択ツール使用時にも適切に設定パネルが開くように `settings` プロパティの受け渡しを調整

## 2026-04-29 12:12
- オブジェクトのレイヤー（重なり順）変更機能を追加
  - 「最前面へ」「最背面へ」「前面へ」「背面へ」の4つの操作を実装
  - 設定パネルにレイヤー操作ボタンを追加（すべての描画ツールおよび選択ツールで利用可能）
  - キーボードショートカットを実装：
    - `Ctrl + Shift + ↑`: 最前面へ
    - `Ctrl + Shift + ↓`: 最背面へ
    - `Ctrl + ↑`: 前面へ
    - `Ctrl + ↓`: 背面へ
  - 選択ツール（Select）使用中も、オブジェクト選択時のみ設定パネルを自動表示するように改善

## 2026-04-29 01:13
- 単一HTML（Single File）ビルド設定を解除し、関連ファイルを削除
  - `vite.config.ts` および `package.json` から `vite-plugin-singlefile` を削除
  - 生成済みの `dist` ディレクトリを削除

## 2026-04-29 01:08
- ステップツールの背景色（円の色）に応じて文字色を自動調整する機能を追加
  - 背景が明るい場合は黒、暗い場合は白の文字色を自動選択する `getContrastColor` ユーティリティを実装
  - ステップ作成時および既存のステップの色変更時に、リアルタイムで文字色が更新されるように調整

## 2026-04-29 01:05
- ステップツールに現在の番号を表示し、編集可能にする機能を追加
  - `useFabric` フックから `stepCount` と `setStepCount` をエクスポートし、外部から制御可能に
  - ステップツールの設定パネルに「Next Step Number」入力フィールドを追加
  - 番号を直接書き換えることで、次に作成されるステップの開始番号を自由に変更できるように実装
  - `App.tsx` 内の分割代入における構文エラー（実装が混入していた箇所）を修正

## 2026-04-29 00:58
- `npm run build` 時の TypeScript エラーを解消。
  - `tsconfig.json` の非推奨設定を修正（Bundler モードへの移行）。
  - Fabric.js v7 の破壊的変更（`pointer` -> `scenePoint` など）に対応。
  - `src/vite-env.d.ts` を追加し、Vite の型定義を有効化。
- `vite-plugin-singlefile` により、JS/CSS がインライン化された単一 HTML ファイル（`dist/index.html`）の生成に成功。

## 2026-04-29 00:52
- アプリのオフライン対応状況を確認。外部API（AI機能など）に依存せず、すべてのライブラリとフォントがローカル/システムリソースで完結しているため、完全にオフラインで使用可能であることを確認。

## 2026-04-29 00:49
- ショートカットキーによるテキスト編集開始機能を追加
  - テキストボックスを選択中に `F2` キーを押すことで、即座に編集モード（テキスト入力状態）に移行できるように実装
## 2026-04-28 23:44
- ペンツールに「直線モード」を追加
  - 設定パネルから「自由描画（Freehand）」と「直線（Straight）」を切り替えられるように実装
  - 直線モード時は、ドラッグ＆ドロップで直線を引けるようにロジックを追加
  - 直線の色、太さ、不透明度、スタイル（破線など）の設定が正しく反映されるように調整

## 2026-04-28 22:53
- フォントサイズ変更が反映されないバグを修正
  - `updateToolSetting` 内の条件分岐に `fontSize` が含まれていなかったミスを修正し、スライダー操作が即座にオブジェクトに反映されるように改善
  - ステップツール（Group）内のテキストに対しても、フォントサイズや装飾設定が適用されるようにロジックを拡張

## 2026-04-28 22:51
- テキストのフォントサイズ調整機能を追加
  - 設定パネルに「Font Size」スライダーを追加し、8px〜100pxの範囲でサイズ変更を可能に
  - テキストツールだけでなく、ステップツールの数字のサイズ変更にも対応
  - 既存のテキストを選択した際に、現在のサイズをスライダーに自動反映させる同期処理を実装
