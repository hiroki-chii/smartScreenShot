# 作業ログ

## 2026-04-28 18:29
- オブジェクトの角度（Angle）調整機能を追加
  - 設定パネルに「Angle」スライダーを追加し、0〜360度の範囲で精密な回転調整が可能に
  - マウスでの直接回転操作だけでなく、スライダーからの操作も全ての図形（矩形、矢印、テキスト、ステップ、ペン）に同期するように実装
  - 既存のオブジェクトを選択した際に、その現在の角度をスライダーに読み込む同期処理を実装

## 2026-04-28 18:21
- ペンツール（自由線）にも動的な破線・点線スタイルを適用
  - ブラシ設定（`freeDrawingBrush`）に `strokeDashArray` を連動させ、描画中もスタイルが反映されるように改善
  - `path:created` イベントで描画後のパスに対してもスタイルと丸い端点設定を適用
  - これにより、矩形、矢印、ペンの全ての図形ツールで一貫したプレミアムな線スタイルが利用可能に

## 2026-04-28 18:19
- 線の太さに連動する動的な破線・点線スタイル（Stroke Style）を実装
  - `strokeWidth` を大きくした際に破線がぼやけたり潰れたりする問題を解決するため、破線パターンを太さに応じて動的にスケーリングするように改善
  - 「Dotted（点線）」スタイルにおいて、`strokeLineCap: 'round'` を使用し、太い線でも綺麗な丸いドットが表示されるように修正
  - 全ての図形（矩形、矢印、自由線）において `strokeLineCap/Join` を `round` に設定し、接合部や端が丸みを帯びた、より洗練された外見に変更
  - 内部状態を `strokeStyle`（solid, dashed, dotted）という抽象化されたスタイル名で管理するように刷新
