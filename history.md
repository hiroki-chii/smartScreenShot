# 作業ログ

## 2026-04-28 16:51
- Escape キーによる選択解除とツールのリセット機能を追加
  - Escape キーを押すと、現在選択されているオブジェクトを解除し、同時にツールを「Select」に切り替えるように修正
- 空白クリック時のリセット挙動と併せて、操作のキャンセルをより簡単に行えるように改善

## 2026-04-28 16:49
- ツールの自動切り替え条件を整理
  - オブジェクト選択時にツールを自動で切り替える機能（以前追加したもの）を廃止。選択しても現在のツール（Rectangleなど）は維持されるように修正
  - キャンバスの空白部分をクリックして選択を解除した際には、自動的に「Select」ツールに切り替わるように設定
  - これにより、意図しないモード変更を防ぎつつ、編集後のリセット操作を簡素化

## 2026-04-28 16:44
- サイドバーでのツール切り替え時にオブジェクトの選択を解除するように改善
  - 別のツールアイコンをクリックした際、現在選択されているオブジェクトの選択を自動で解除し、スムーズに新しい図形の描画モードへ移行できるように修正
  - ただし、すでにアクティブなツールのアイコンを再度クリックした場合は、選択を維持したまま設定パネルでの編集を継続可能
  - 「Select」ツールをクリックした際も同様に選択を解除するように設定

## 2026-04-28 16:36
- 既存のオブジェクト操作時に新規オブジェクトが作成されるバグを修正
  - ツールがアクティブな状態でも、既存のオブジェクト（矩形など）をクリックした場合は新規作成を行わず、選択や移動を優先するように挙動を修正
  - これにより、オブジェクト選択時に「ゴミ」のような小さなオブジェクトが生成される問題を解決

## 2026-04-28 16:31
- オブジェクト選択時の自動ツール切り替えに合わせて、設定パネルも自動で開くように改善
  - オブジェクトを選択すると、該当ツールのアイコンがアクティブになるだけでなく、色や太さを変更できる設定ウィンドウが即座に表示されるように修正
  - App.tsx で Popover の表示状態を管理することで、自動・手動の両方のツール選択において一貫した挙動（設定パネルの表示）を実現
- これにより、オブジェクトをクリックしてそのままダイレクトに見た目を調整できるようになった

## 2026-04-28 16:28
- 選択したオブジェクトに応じてツールを自動切り替えする機能を追加
  - キャンバス上のオブジェクト（矩形、矢印など）を選択すると、サイドバーの該当するツールアイコンが自動的にアクティブになるように変更
  - 何も選択していない状態（選択解除時）には、自動的に「Select」ツールに戻るように調整
  - これにより、オブジェクトを選択した直後にそのまま設定パネルで編集できるスムーズなフローを実現

## 2026-04-28 16:20
- ツール設定パネルの表示トリガーを「ホバー」から「クリック」に変更
  - アイコンにマウスを合わせただけでは設定が出ないようにし、クリックしたときにツール選択と同時に設定が開くように挙動を修正
- レクタングルの塗りつぶしの不透明度（Fill Opacity）が正しく反映されないバグを修正
  - 常に最新のカラー設定から RGBA を再生成するようにロジックを改善

## 2026-04-28 16:05
- Rectangle ツールの大幅な機能拡張と Blur ツールの廃止
  - 外枠（Stroke）と塗りつぶし（Fill）を個別に設定できる機能を追加
    - それぞれの色（Color）と不透明度（Opacity）を独立して調整可能に
    - 塗りつぶしの色に「透明（None）」オプションを導入
  - 枠線スタイル（Stroke Style）の選択機能を追加
    - 実線（Solid）、破線（Dashed）、点線（Dotted）を切り替え可能
  - ぼかし（Blur）機能を廃止し、ツールバーから削除
- 設定パネルのUIを刷新し、ツールに応じた適切なコントロール（塗りつぶし、枠線スタイルなど）を表示するように改善

## 2026-04-28 15:55
- オブジェクトの「後出し変更」機能を実装
  - キャンバス上のオブジェクトを選択した状態で、ツール設定パネル（色、不透明度、線の太さ）を変更すると、即座にオブジェクトに反映されるように修正
  - 逆に、オブジェクトを選択した際に、そのオブジェクトの設定（色など）をツール設定パネルに自動で同期する機能を追加
  - Rect, Arrow, Text, Step, Pen の各オブジェクトタイプごとに適切なプロパティ同期ロジックを実装
  - 変更時の履歴保存（Undo/Redo対応）を統合

## 2026-04-28 15:45
- 各ツール（矩形、矢印、テキスト、ステップ、ペン）ごとに個別の設定（色、不透明度、線の太さ）を保持できるように変更
- ツールバーのアイコンにホバーした際に設定パネルが表示される機能を追加
  - Popover と Slider (shadcn/ui) を使用したプレミアムなUIを実装
  - 選択中のツールの色と不透明度がアイコンの右下にインジケーターとして表示されるように改善
- `useFabric` フックを拡張し、ツールごとの `toolSettings` 状態管理と `updateToolSetting` 関数を導入
- ペンツール（ブラシ）の不透明度対応と、描画後のオブジェクトへの不透明度反映ロジックを実装
- ツール設定パネルの表示にわずかな遅延を設けることで、操作性を向上（誤操作防止）

## 2026-04-28 12:24
- ズーム（拡大・縮小）に合わせてキャンバスの枠（影の表示エリア）も連動してサイズが変わるように修正
  - `useFabric` 内で `baseSize` を保持し、ズーム倍率を掛け合わせたサイズを `setDimensions` でキャンバス要素に適用
  - これにより、拡大時に画像が枠にクリップされず、影も正しく画像の外側に表示されるように改善

## 2026-04-28 12:15
- 画像の外側（キャンバス外の背景エリア）をクリックした際に選択状態を解除する機能を追加
  - `useFabric` フックに `deselectAll` 関数を追加
  - `App.tsx` のキャンバスコンテナに `onMouseDown` イベントを実装し、背景クリック時に `deselectAll` を実行するように修正
- GitHub リポジトリへのプッシュ完了 (main ブランチ)

## 2026-04-27 21:26
- GitHub リポジトリへの初回プッシュ完了 (main ブランチ)
- Undo (元に戻す) & Redo (やり直し) 機能の実装
- Fabric.js の状態保存 (JSON) による履歴管理ロジックの導入
- ヘッダーに Undo/Redo ボタンを追加 (Copyボタンの左側)
- Tooltip による操作補助の追加
- Delete / Backspace キーによるオブジェクト削除機能の実装
- ダークモード / ライトモード切り替え機能の実装（システム設定連動対応）
- UI 全体を shadcn/ui のテーマ変数に準拠するようにリファクタリング
- キャンバス周りの不要な枠線、角丸、初期背景色（白）を削除
- 画像未貼り付け時の透明化と、画像貼り付け時のみ影を表示する動的スタイルの導入
- Clear All 時に背景が白くなってしまう問題を修正（透明を維持するように）
- Undo / Redo 時に背景画像の有無を再確認し、影の表示を正しく同期するように修正
- ショートカットキーの実装 (Ctrl + Z: Undo, Ctrl + Y: Redo)

## 2026-04-27 19:28
- プロジェクト初期化 (Vite + React + TS)
- ScreenPresso クローンアプリの開発開始
- Tailwind CSS v3 & shadcn/ui のセットアップ完了
- alias (@/*) 設定完了 (tsconfig.json 直接記述)
- Fabric.js v7 インストール & 本格実装完了
- 描画ロジック (矢印、矩形、ステップ、テキスト) の安定化完了
- 保存 (PNG) & クリップボードコピー機能の実装
- UI 改善 (ScreenPresso 風ダークテーマ & プレミアムレッド)
- 画像貼り付け時の位置ズレ問題 (右上へのオフセット) を修正
- キャンバスコンテナのレイアウト安定化
- ツール使用時のデフォルト範囲選択 (Selection) を無効化
- オブジェクト作成後の選択状態の安定化
